{
  "name": "free-fast-mode",
  "version": "0.1.0",
  "description": "free-fast-mode: a performance layer for coding agents",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "docs"
  ],
  "bin": {
    "ffm": "dist/cli/index.js",
    "free-fast-mode": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .ffm-cache benchmarks/latest",
    "test": "npm run build && node --test dist/**/*.test.js",
    "serve": "npm run build && node dist/cli/index.js serve",
    "status": "npm run build && node dist/cli/index.js status",
    "snapshot": "npm run build && node dist/cli/index.js snapshot",
    "bench": "npm run build && node dist/cli/index.js bench",
    "clear-cache": "npm run build && node dist/cli/index.js clear-cache",
    "plugin": "npm run build && node dist/cli/index.js plugin scaffold"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.6.3"
  }
}
